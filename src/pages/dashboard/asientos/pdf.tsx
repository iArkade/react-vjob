import * as React from 'react';

export function Page(): React.JSX.Element {
     return (
          <>
               <Document>
                    
               </Document>
          </>
     )
}